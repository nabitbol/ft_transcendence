HOME_PATH=/mnt/nfs/homes/nabitbol/ft_transcendence

NX_CONTAINER_FILES_PATH=/app

# SHARED
NX_HOST_NAME=
CONTAINER_HOST= 0.0.0.0
NX_SOCKET_GAME=3030
NX_SOCKET_CHAT=8080

# BACKEND
NX_JWT_SECRET=testSecreatString
NX_API_URL=
NX_CLIENT_ID=
NX_CLIENT_SECRET=
NX_BACKEND_PORT=3333

# FRONTEND
NX_FRONTEND_PORT=4200

# Database
POSTGRES_PASSWORD=postgres
POSTGRES_USER=postgres
DB_PORT=5432
DB_NAME=postgres
DB_HOST=postgres
DATABASE_URL=${DB_NAME}://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}
